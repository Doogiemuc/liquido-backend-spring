# Lombok configuration for all the files in this dir (and below)

# this is necessary to prevent the error "no String-argument constructor/factory method to deserialize from String value"
lombok.anyConstructor.suppressConstructorProperties=true